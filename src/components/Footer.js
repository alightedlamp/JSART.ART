import React, { Component } from 'react'

class Footer extends Component {
    render() {
        return(
            <footer>
                RESET
            </footer>
        )
    }
}

export default Footer;